[package]
name = "scratch-engine"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = true

[dev-dependencies]
serde_json = "1"
